# Personal Access Token configuration
Create a Fine-grained personal access token with the organisation as the resource owner.

It requires:
- `Read and write` on `Administration`
- `Read and write` on `Members`